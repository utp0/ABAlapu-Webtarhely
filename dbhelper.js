function getRowFromResult(result, rowname) {
    if(typeof rowname === 'undefined') throw new Error("Nincs oszlopnév megadva!")
    if(typeof result !== 'object') throw new Error("Hibás adatb. válaszformátum! (result obj)")
    try {
        //result.metaData.
    } catch (e) {

    }
}